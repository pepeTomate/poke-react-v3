export interface Pokemon {
    id: number;
    name: string;
    form_descriptions: string;
    generation: string;
    sprites: string;
};

export interface PokemonArray {
    name: string;
    url: string;
}